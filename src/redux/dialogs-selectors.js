export const selectMessages = (state) => state.dialogsPage.messages;

export const selectNewMessageText = (state) => state.dialogsPage.newMessageText;

export const selectDialogs = (state) => state.dialogsPage.dialogs;
