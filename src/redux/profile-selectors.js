export const selectCurrentProfile = (state) => state.profilePage.currentProfile;

export const selectCurrentStatus = (state) => state.profilePage.currentStatus;

export const selectPosts = (state) => state.profilePage.posts;

export const selectNewPostText = (state) => state.profilePage.newPostText;

export const selectProfileImage = (state) => state.profilePage.currentImage;
